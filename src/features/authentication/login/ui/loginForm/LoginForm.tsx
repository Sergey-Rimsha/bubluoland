import React, { ReactElement } from 'react';

import s from 'features/authentication/login/ui/loginForm/LoginForm.module.scss';

export const LoginForm = (): ReactElement => {
  return <div className={s.form}>loginform</div>;
};
